#include "main.h"
/**
 * print_alphabet - it prints alphabet
 *
 * Return: Always Alphabet
 */
void print_alphabet(void)
{
	char a;

	for (a = 'a' ; a <= 'z' ; a++)
	{
		_putchar(a);
	}
	_putchar('\n');
}
